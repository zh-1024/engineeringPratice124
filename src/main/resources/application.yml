spring:
  datasource:
    username: root
    password: 990913
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/photo
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
